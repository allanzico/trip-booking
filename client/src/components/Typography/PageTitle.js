import React from 'react'

const PageTitle = ({ children }) => {
  return (
    <h1 className="text-2xl font-semibold text-orange-500 uppercase">{children}</h1>
  )
}

export default PageTitle